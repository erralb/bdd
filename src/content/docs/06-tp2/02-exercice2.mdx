---
title: Les employés - Exercice 2
sidebar:
    label: Exercice 2
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Slide from '/src/components/Slide.astro';

Soit le schéma relationnel suivant :

import {Content as Employes} from '/src/content/files/employes-text.md'

<Employes />


<details>
    <summary>Diagramme</summary>

import {Content as EmployesERD} from '/src/content/files/employes-mermaid.md'

<EmployesERD />

</details>

<details>
    <summary>Données</summary>

import DBEmployes from "/src/components/DBEmployes.astro";

<DBEmployes />

</details>

Téléchargement de la base : [employes.sqlite](/bdd/sample/employes.sqlite)

# Exercice 2 : Requêtes SQL

Pour chaque requête de l'exercice 1, donner la ou les réponse(s) correspondante(s) en SQL. 

Pour les requêtes `10`, `11` et `12`, on pourra définir des [`vues`](https://www.sqlite.org/lang_createview.html) qui permettent de définir différents rangs : _rang 1 pour le boss, rang 2 pour ses employés directs, etc._

Une `vue` (`view` en anglais) est une requête SFW stockée sous forme de table temporaire. Une vue peut être requêtée comme une table classique avec des requêtes SFW.


1. Donner le `nom` de tous les `employés`.

2. Donner le `nom` et la `date` d'embauche des `employés` du département `20`.

3. Donner tous les `salaires` perçus par les `employés` de l'entreprise.

4. Donner le `nom` et le `numéro` du `département` des `employés` travaillant à `Dallas`.

5. Donner le `nom` et le `salaire` des `employés` dont le `nom` commence par un `M` et dont le `salaire` est supérieur à `1290$`.

6. Donner les `départements` employant des `CLERK`, `SALESMAN` et des `ANALYST`.

7. Donner le `nom des chefs` dont les `employés` perçoivent des `commissions`.

8. Donner le `nom` et le `salaire` des `employés` des `départements` de `Chicago` et `Dallas` dont le `salaire` est supérieur à `1000$`.

9. Donner le `nom` des `employés` qui gagnent plus que leur `chef`.

10. Donner la `hiérarchie` de l'entreprise

11. Donner le `nombre` d'employés `par niveau` de hiérarchie.

12. Donner la `moyenne` des `salaires` `par niveau` de hiérarchie.
