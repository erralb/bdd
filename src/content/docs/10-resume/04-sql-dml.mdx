---
title: SQL Data Manipulation Language (DML)
description: Langage SQL et langage de manipulation de données
sidebar:
  label: DML
---

import Slide from "/src/components/Slide.astro";

<Slide>

Dans la partie sur la gestion des bases de données, nous avons abordé le [SQL DML](/bdd/07-sql3/02-sql-dml/)

## INSERT

```sql

INSERT INTO table (colonne1, colonne2, ...)
VALUES (valeur1, valeur2, ...)

```

### From a SELECT
  
```sql
INSERT INTO table (colonne1, colonne2, ...)
SELECT colonne1, colonne2, ...
FROM table
WHERE condition
```

</Slide>

<Slide>

## UPDATE
  
```sql
UPDATE table
SET colonne1 = valeur1, colonne2 = valeur2, ...
WHERE condition
```

</Slide>

<Slide>

## DELETE

```sql
DELETE FROM table
WHERE condition
```

</Slide>
