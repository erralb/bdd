---
export interface Props {
  centered?: boolean
  highlight?: boolean
  large?: boolean
  title?: string
}

const { centered, highlight, large, title} = Astro.props
---

<section class="presentation-slide centered large" class:list={{ centered, highlight, large }}>
  {title? <h1 class="presentation-slide-title">{title}</h1> : null}
  <slot />
</section>
